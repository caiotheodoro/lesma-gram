name: login_prog_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you intend to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.10.0
  firebase_auth: ^4.7.0
  google_sign_in: ^6.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  # Adicione outras dependências de desenvolvimento aqui, se necessário
  # exemplo:
  # pedantic: ^1.11.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/image.png
